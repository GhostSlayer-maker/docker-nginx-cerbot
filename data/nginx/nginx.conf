server {
    listen 80;
    server_name bstockmanager.com test.bstockmanager.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    location / {
        return 404;  # O puedes redirigir a una página de mantenimiento
    }
}